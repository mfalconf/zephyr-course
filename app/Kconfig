config APP_HEARTBEAT_PERIOD_MS
    int "Heartbeat LED period (ms)"
    default 500
    range 100 2000
    help
        Sets the blink period for the heartbeat LED.

source "Kconfig.zephyr"
rsource "drivers/our_driver/Kconfig"
